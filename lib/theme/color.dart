import 'package:flutter/material.dart';

class AppColor {
  static const primary = Color(0xFFC00000);
  static const secondary = Color(0xFFED1C24);
  static const grayDrawer = Color(0xFF5D7285);
  static const grayTextColor = Color(0xFF6B6D6F);
  static const white = Colors.white;
  static const pageColor = Color(0xFFFCFCFC);
  static const colorButtonWpp = Color(0xFF1FAF38);
  static const colorShadow = Color.fromARGB(82, 73, 73, 73);
  static const borderRedTransparent = Color.fromARGB(130, 192, 0, 0);

  static const listColors = [
    primary,
    secondary,
    grayDrawer,
    grayTextColor,
    white,
    pageColor,
    colorButtonWpp,
    colorShadow
  ];
}
